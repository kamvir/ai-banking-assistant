{
  "name": "ai-banking-assistant",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "lint": "npm run lint --workspace=mobile-web && npm run lint --workspace=api",
    "build": "npm run build --workspace=mobile-web && npm run build --workspace=api",
    "test": "npm run test --workspace=api",
    "start:web": "npm run start --workspace=mobile-web",
    "start:api": "npm run start:dev --workspace=api"
  },
  "devDependencies": {
    "prettier": "^3.3.3"
  }
}
